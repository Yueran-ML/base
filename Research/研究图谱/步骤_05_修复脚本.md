---
tags: [步骤, Stage0]
date: 2026-03-27
---

# 步骤 05：修复 stage0_metric_validation.py（7 处关键修改）

**前置步骤**: [[步骤_04_StepA基线相图]]
**后续步骤**: [[步骤_06_StepA结果确认]]

---

## 修改 1：替换 compute_procrustes_ring → compute_circle_score

**旧**：PCA top-2 + Procrustes 对齐 → 全部为 0（见 [[步骤_02_Stage0v1运行]]）

**新**：[[概念_CircleScore]] — 全维度平行四边形条件，不依赖降维

## 修改 2：CellResult 字段重命名

`procrustes_r2` → `circle_score`

## 修改 3：训练循环指标替换

```python
# 旧
r2 = compute_procrustes_ring(emb, prime)
# 新
cs = compute_circle_score(emb, prime)
```

## 修改 4：新增 _find_tau_sustained

```python
def _find_tau_sustained(steps, values, threshold, n_sustained=3):
    for i in range(len(steps) - n_sustained + 1):
        if all(values[i + k] >= threshold for k in range(n_sustained)):
            return float(steps[i])
    return None
```

设计理由：要求连续 3 个 checkpoint（= 1500 步，远小于 Grokking 时间尺度）稳定维持，避免噪声假阳性。

## 修改 5：修复 tau_gen 定义

```python
# 旧：估计测试 NLL 变化点 → 在 ~5000 步误触发
# 新：测试准确率首次持续 ≥0.9 的步数
result.tau_gen = _find_tau_sustained(result.steps, result.test_acc, threshold=0.9)
```

## 修改 6：修复 tau_ring 定义

```python
# 旧：对全为 0 的 procrustes_r2 做变化点估计
# 新：CS ≥ 0.8 持续；若从未达到，用变化点作备用
result.tau_ring = _find_tau_sustained(result.steps, result.circle_score, threshold=0.8)
if result.tau_ring is None:
    result.tau_ring = estimate_changepoint(result.steps, result.circle_score)
```

## 修改 7：更新 P2 通过标准

`R(t) < 0.15 at init` → `Circle Score < 0.05 at init`

---

## 注意

这些修复都在 [[步骤_01_Stage0脚本创建]] 创建的脚本基础上修改。后来在 [[步骤_17_统一分类器]] 中又进一步将共享函数提取到 `grok_metrics.py`。
